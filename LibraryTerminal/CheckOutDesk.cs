using System;
using System.Collections.Generic;
using System.Text;

namespace LibraryTerminal
{
    class CheckOutDesk
    {
        //VerifyCheckout(Media)
        //Checkout(Media)

        //Return() - check if dueDate is after today (DateTime .GreaterThan (dueDate, DateTime.Today), change boolCheckout to false
    }
}
